name: Character Crawler
on:
  schedule:
    - cron: '0 12 * * *'  # KST 21:00 (pronews/ts와 시간 분리)
  workflow_dispatch:
jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run crawler
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python character_crawler.py

      - name: Save history to repo
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add posted_articles_character.json
          git diff --cached --quiet || git commit -m "update character history [skip ci]"
          git push
