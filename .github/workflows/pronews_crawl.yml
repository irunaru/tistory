name: ProNews Crawler
on:
  schedule:
    - cron: '0 21 * * *'  # KST 06:00 매일
  workflow_dispatch:
jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run crawler
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python pronews_crawler.py

      - name: Save history to repo
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add posted_articles_pronews.json || true
          git diff --cached --quiet || git commit -m "update pronews history [skip ci]"
          git push || true
